const colors = ["red", "green", "blue", "yellow", "purple"];
 
//your task is to use array destructuring to extract the first three colors from the colors array and assign them to seperate variables named color1, color2 and color3 respectively.
const [color1,color2,color3] = colors;
//after extracting the colors, log them to the console.
console.log(color1);
console.log(color2);
console.log(color3);

