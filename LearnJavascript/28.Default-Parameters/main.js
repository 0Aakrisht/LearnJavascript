//1. create function name (multiply) with 2 parameters (a,b) which will multiply the parameters values and return the result.
//2. provide 1 as the default value to parameter "b".
function multiply(a , b = 1) {
    console.log(a*b);
}
multiply(10);
//then the output will be 10 